{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "postinstall": "node scripts/ensure-tina-env-symlink.mjs && node scripts/install-githooks.mjs",
    "check:tina": "node scripts/check-tina-admin.mjs",
    "check:tina:staged": "node scripts/check-tina-admin.mjs --staged",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@google-analytics/data": "^6.1.0",
    "@supabase/supabase-js": "^2.99.3",
    "@vercel/node": "^5.8.26",
    "@workspace/checkout": "workspace:*",
    "@workspace/insights": "workspace:*",
    "resend": "^6.17.2",
    "stripe": "^20.4.1"
  }
}
